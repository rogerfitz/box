{% if users %}
    <ul>
    {% for product in products %}
        <li><a href={{ product.url }}>Order</a></li>
	<li>{{ product.name }}</li>
	<li>${{ product.price }}</li>
	<li>{{ product.why_buy }}</li>
	<li>{{ product.category }}</li>
	<li><img src={{ product.image_url }} height="200" width="150"></li>
	<br>
    {% endfor %}
    </ul>
{% else %}
    <p>No polls are available.</p>
{% endif %}
